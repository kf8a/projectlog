<%= form_for :entry, url: {action: "update"}, method: "put",  html: {class: "form", role: "form"}  do |f| %>
  <%= render partial: "form", locals: {f: f} %>
  <%= f.submit "Save", class: "btn btn-primary pull-down" %>
<% end %>
