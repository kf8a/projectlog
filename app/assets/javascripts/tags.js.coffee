# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

jQuery ->
  # tagOptions =
  #   name: 'tags',
  #   remote: '/tags.json?search=%QUERY&context=categories',
  #   queryTokenizer: Bloodhound.tokenizers.whitespace,
  #   datumTokenizer: (d) -> return Bloodhound.tokenizers.whitespace(d.name)

  # engine = new Bloodhound(tagOptions)

  # promise = engine.initialize()

  # jQuery("#entry_category_list").typeahead(
  #   null, { displayKey: 'name',  source: engine.ttAdapter() })
